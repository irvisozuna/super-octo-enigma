// Generated by DDD Module Generator on 2025-07-04

import { computed } from 'vue'
import { useConnectionStore } from '../stores/connectionStore'

export function useConnection() {
  const store = useConnectionStore()

  const isLoading = computed(() => store.loading)
  const hasError = computed(() => !!store.error)
  const errorMessage = computed(() => store.error)

  const connections = computed(() => store.items)
  const currentConnection = computed(() => store.currentItem)

  const pagination = computed(() => ({
    page: store.currentPage,
    perPage: store.perPage,
    total: store.total,
    totalPages: store.totalPages,
  }))

  return {
    // Store
    store,

    // Computed
    isLoading,
    hasError,
    errorMessage,
    connections,
    currentConnection,
    pagination,

    // Actions
    fetchList: store.fetchList,
    fetchById: store.fetchById,
    createItem: store.createItem,
    updateItem: store.updateItem,
    deleteItem: store.deleteItem,
    setPage: store.setPage,
    clearError: store.clearError,
  }
}
