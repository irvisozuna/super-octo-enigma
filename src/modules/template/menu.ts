import type { VerticalNavItems } from '@layouts/types'

const templateMenu: VerticalNavItems = [
  {
    title: 'Templates',
    icon: { icon: 'tabler-calendar' },
    to: 'templatesList',
    action: 'read',
    subject: 'Templates',
  }
]

export default templateMenu
